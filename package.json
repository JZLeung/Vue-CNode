{
  "name": "vue-cnode",
  "version": "0.0.0",
  "description": "CNode",
  "private": true,
  "keywords": [],
  "scripts": {
    "dev": "cooking watch -p",
    "dist": "cooking build -p",
    "deploy": "cooking build --no-color",
    "test": "cooking test karma start --single-run"
  },
  "license": "ISC",
  "dependencies": {
    "vue": "^1.0.0",
    "vue-resource": "^1.0.3",
    "vue-router": "^0.7.13"
  },
  "devDependencies": {
    "vue-loader": "^8.0.0"
  }
}
